<h1>監視サイト</h1>

<% form_for [ :confirm, @site ] do |f| %>
	<%= f.error_messages %>
	<fieldset>
		<legend>新規登録</legend>
		<%= render :partial => 'form', :locals => { :f => f } %>
	</fieldset>
<% end %>
